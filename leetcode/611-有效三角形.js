/*** 
 * 给定一个包含非负整数的数组，你的任务是统计其中可以组成三角形三条边的三元组个数。
示例 1:

输入: [2,2,3,4]
输出: 3
解释:
有效的组合是: 
2,3,4 (使用第一个 2)
2,3,4 (使用第二个 2)
2,2,3

构成三角形三边的前提条件是任意俩边之和大于第三边，任意俩边之差小于第三边
*/

/**
 * @param {number[]} nums
 * @return {number}
 */
var triangleNumber = function(nums) {

};